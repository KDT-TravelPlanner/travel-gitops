apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# root-app-kind-dev(argocd/root-app-kind-dev.yaml)이 이 디렉터리를 가리킨다.
resources:
  - ingress-nginx.yaml
  - platform.yaml
  - identity.yaml
  - community.yaml
  - travel.yaml
  - maps.yaml
