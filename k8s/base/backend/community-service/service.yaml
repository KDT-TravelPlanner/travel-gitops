apiVersion: v1
kind: Service
metadata:
  name: community
  labels:
    app: community
spec:
  type: ClusterIP
  selector:
    app: community
  ports:
    - name: http
      port: 8080
      targetPort: http
    - name: actuator
      port: 9091
      targetPort: actuator
