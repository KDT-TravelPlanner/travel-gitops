apiVersion: v1
kind: Service
metadata:
  name: maps
  labels:
    app: maps
spec:
  type: ClusterIP
  selector:
    app: maps
  ports:
    - name: http
      port: 8080
      targetPort: http
    - name: actuator
      port: 9091
      targetPort: actuator
