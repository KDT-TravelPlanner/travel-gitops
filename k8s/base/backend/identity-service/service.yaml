apiVersion: v1
kind: Service
metadata:
  name: identity
  labels:
    app: identity
spec:
  type: ClusterIP
  selector:
    app: identity
  ports:
    - name: http
      port: 8080
      targetPort: http
    - name: actuator
      port: 9091
      targetPort: actuator
