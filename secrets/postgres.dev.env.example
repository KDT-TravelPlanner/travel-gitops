# cp secrets/postgres.dev.env.example secrets/postgres.dev.env 후 값을 채운다.
# secrets/*.dev.env 는 .gitignore 됨 — 커밋 금지.
# -> Secret: postgres-secret  (postgres + identity/community/travel/maps 가 참조)
POSTGRES_DB=travel_diary_dev
POSTGRES_USER=travel_diary
POSTGRES_PASSWORD=travel_diary_password
