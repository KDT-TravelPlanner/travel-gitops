# -> Secret: identity-oauth-secret  (identity 만 참조)
# 로컬 Kind PoC 에서는 비워둬도 기동됨 (secretKeyRef.optional: true).
# 실제 OAuth 로그인 테스트를 할 때만 채운다.
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
NAVER_OAUTH_CLIENT_ID=
NAVER_OAUTH_CLIENT_SECRET=
