# -> Secret: jwt-secret  (identity/community/travel/maps 전부 참조)
# HS256 공유 서명 키 — 최소 32 bytes. identity 가 발급, 나머지가 검증.
# 발급자(issuer) 검증도 일치해야 하며, issuer 는 비민감이라 각 서비스 ConfigMap 에서
# JWT_ISSUER=identity-service 로 고정한다(여기 아님).
JWT_SECRET=local-development-jwt-secret-change-me-please
